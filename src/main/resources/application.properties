# JDBC Driver name.
spring.datasource.driver-class-name=org.postgresql.Driver
# Database connection string URL.
# Postgre SQL Server name: linux_test
spring.datasource.url=jdbc:postgresql://192.200.100.221:5432/electro_order
# Database user's name.
spring.datasource.username=postgres
# Database user's password.
spring.datasource.password=postgres
# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=50
# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults= false

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.generate-ddl=true
server.port=8080
#########################
#spring.jpa.show-sql = true

#logging.file=${catalina.base}/logs/elektrik_log.log
#logging.level.root=INFO
#logging.level.org.springframework.web=WARN
#logging.level.com.konex.servapp=INFO
#logging.level.org.hibernate=INFO
#logging.level.org.hibernate.SQL=INFO
#logging.pattern.file="%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
